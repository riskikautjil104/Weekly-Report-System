services:
  waha:
    image: devlikeapro/waha:latest
    platform: linux/amd64
    container_name: weekly-report-waha
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./docker/waha.env
    volumes:
      - ./docker/waha-sessions:/app/.sessions
